info:
  name: Refresh Access Token
  type: http
  seq: 6
  tags:
    - Authentication

http:
  method: POST
  url: "{{baseUrl}}/api/users/token/refresh/"
  body:
    type: json
    data: |-
      {
        "refresh": "",
        "platform": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/users/token/refresh/"
      method: POST
      body:
        type: json
        data: |-
          {
            "refresh": "",
            "platform": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "access": "",
            "refresh": ""
          }
