info:
  name: Update client details (Superuser only)
  type: http
  seq: 5
  tags:
    - Clients

http:
  method: PUT
  url: "{{baseUrl}}/api/clients/:id/"
  params:
    - name: id
      value: ""
      type: path
      description: A UUID string identifying this reviewed client.
  body:
    type: json
    data: |-
      {
        "phone_number": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/clients/:id/"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
          description: A UUID string identifying this reviewed client.
      body:
        type: json
        data: |-
          {
            "phone_number": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "phone_number": ""
          }
